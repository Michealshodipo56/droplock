# DropLock Development Roadmap 🚀

This list tracks the remaining tasks for the DropLock privacy-first storage platform.

## File transfer logic
now the scan scanning should only show device that are online in the site right now it should use the name generated for the device that what would show in the scanning,
whenever a user scans for a device it shows them the people that are online and have the site open, then the user can select a device to send a file to, the file should go the backend and the device that the user wants to send it to should be the one thats able to download it, so it feels like it is a direct connection between the two devices.

## device name logic
when a the site is opened on a device it should generate a random name for the device, this name should be displayed on the site and should be used to identify the device, thats how device find each other in the scan.

## locker security logic
when a user enter a name and clicks on the access locker i shows a pop up that shows if the locker exists it should show a message above the input field that says the locker exists and it should ask for password and if it dosent it should just show a message abouve the password field that says the locker hasent been used enter a password to create it, and it takes them to the locker page.


- recent vault, when the user clicks on the recent vault button it should show them a list of all the vaults they have visited, and they can click on any of them to access it, but itll ask for password again.

